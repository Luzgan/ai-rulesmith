---
name: Descriptive Naming
description: Naming conventions that convey purpose and intent
category: code-style
tags: [naming, readability, conventions]
---

# Descriptive Naming

- Names convey purpose — reader should understand without context
- `camelCase`: variables, functions, methods
- `PascalCase`: classes, interfaces, types, enums
- `UPPER_SNAKE_CASE`: constants
- `kebab-case`: file and directory names
- Booleans: `is`, `has`, `can`, `should` prefix
- Functions: `verb` + `noun` (e.g., `getUser`, `validateInput`)
- Collections: plural nouns
- Avoid single-letter vars, generic names (`data`, `temp`)
